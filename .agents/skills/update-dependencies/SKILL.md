---
name: update-dependencies
description: Permit to update Maven dependencies
---

# How to proceed

You must launch the script provided in the `scripts` folder.

It will provide a list of dependencies to update.

You can update the dependencies with the same major version without asking the user.
Otherwise, you must ask the user for confirmation.

After updating the dependencies, you must clean the obsolete maven properties.